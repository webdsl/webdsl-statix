module keywords/template-calls

language webdsl-statix

fixture [[
  application keywords_template_calls

  template t() {
    [[...]]
  }
]]

test template id allowed [[ t ]] parse succeeds parse to TemplateCall("t", OptTCallArgsNone(), OptTCallPropAssignsNone(), TemplateBodyNone())
test template id define not define [[ define ]] parse fails
test template id define not template [[ template ]] parse fails
test template id define not var [[ var ]] parse fails
test template id define not for [[ for ]] parse fails
test template id define not if [[ if ]] parse fails
test template id define not else [[ else ]] parse fails
test template id define not as [[ as ]] parse fails
test template id define not is [[ is ]] parse fails
test template id define not null [[ null ]] parse fails
test template id define not and [[ and ]] parse fails
test template id define not or [[ or ]] parse fails
test template id define not order [[ order ]] parse fails
test template id define not by [[ by ]] parse fails
test template id define not where [[ where ]] parse fails
test template id define not in [[ in ]] parse fails
test template id define not navigate [[ navigate ]] parse fails
test template id define not action [[ action ]] parse fails
test template id define not submit [[ submit ]] parse fails
test template id define not goto [[ goto ]] parse fails
test template id define not foreach [[ foreach ]] parse fails
test template id define not request [[ request ]] parse fails
test template id define not local [[ local ]] parse fails
test template id define not init [[ init ]] parse fails
test template id define not databind [[ databind ]] parse fails
test template id define not derive [[ derive ]] parse fails
test template id define not rules [[ rules ]] parse fails
test template id define not actionLink [[ actionLink ]] parse fails
test template id define not submitlink [[ submitlink ]] parse fails
test template id define not downloadlink [[ downloadlink ]] parse fails
test template id define not outputimage [[ outputimage ]] parse fails
test template id define not placeholder [[ placeholder ]] parse fails
test template id define not placeholdervar [[ placeholdervar ]] parse fails
